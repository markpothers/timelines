<h1><%=@person.name%></h1>

  <ul>
    <li>
      <%= @person.birthday%>
    </li>
  </ul>

<%=form_tag("/people/#{@person.id}/delete", method: "get") do |f| %>
  <%=submit_tag "Edit"%>
<%end%>
<%=form_tag("/people/#{@person.id}", method: "delete") do |f| %>
  <%=submit_tag "Delete"%>
<%end%>